# Google AI Studio Configuration
# Users can configure their API key through the Settings modal in the app
# This fallback is used if no API key is configured in the UI
VITE_AI_API_KEY=your_google_ai_studio_api_key_here